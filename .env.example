# Copy to .env and fill in the keys for the agents you want to field.
# Agents with a missing key are skipped (or run all agents with --mock).
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
GEMINI_API_KEY=
XAI_API_KEY=
